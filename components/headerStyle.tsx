import { StyleSheet } from 'react-native'
import React from 'react'

const styles = StyleSheet.create({
    headerText: {
        paddingTop: 10,
        fontFamily: "Press2P",
        fontSize: 20,
    }
     
});

export default styles.headerText; 